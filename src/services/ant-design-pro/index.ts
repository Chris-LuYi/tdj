// @ts-ignore
/* eslint-disable */
// API 更新时间：
// API 唯一标识：
import * as api from './api';
import * as login from './login';
enum Direction {
  Up,
  Down,
  Left,
  Right,
}
console.log(1);
console.log(Direction.Up);
export default {
  api,
  login,
  Direction,
};
